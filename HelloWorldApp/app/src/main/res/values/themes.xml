<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HelloWorldApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/my_light_primary</item>
        <item name="android:textViewStyle">@style/CustomTextView</item>

    </style>

    <style name="Theme.HelloWorldApp" parent="Base.Theme.HelloWorldApp" />
    <style name="CustomTextView" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/poppinsmedium</item> <!-- ganti dengan jenis font yang Anda inginkan -->
    </style>
</resources>